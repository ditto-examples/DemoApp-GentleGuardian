import SwiftUI

/// Sheet for logging a custom/other vaccine — stub, implemented in Task 13.
struct LogOtherVaccineSheet: View {
    let vaccinationRepository: VaccinationRepository
    let child: Child?

    var body: some View {
        Text("Log Other Vaccine — Coming in Task 13")
    }
}
