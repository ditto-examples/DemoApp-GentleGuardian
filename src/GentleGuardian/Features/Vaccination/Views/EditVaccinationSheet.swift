import SwiftUI

/// Sheet for editing an existing vaccination record — stub, implemented in Task 13.
struct EditVaccinationSheet: View {
    let record: VaccinationRecord
    let vaccinationRepository: VaccinationRepository
    let child: Child?

    var body: some View {
        Text("Edit Vaccination — Coming in Task 13")
    }
}
