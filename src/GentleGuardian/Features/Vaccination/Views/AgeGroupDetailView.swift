import SwiftUI

/// Detail view for an age group's vaccines — stub, implemented in Task 12.
struct AgeGroupDetailView: View {
    let group: AgeGroup
    let vaccinationRepository: VaccinationRepository
    let vaccinationViewModel: VaccinationViewModel

    var body: some View {
        Text("Age Group Detail — Coming in Task 12")
    }
}
